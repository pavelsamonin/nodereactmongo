import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';
ReactDOM.render(
  <App headerMessage='Hello from component!' />,
  document.getElementById('root')
);
